name: 🕵️ Debug CI

on: [push, pull_request]

jobs:
  investigate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: 📂 List ALL Files (Recursive)
        run: |
          echo "=== ROOT DIRECTORY ==="
          ls -la
          echo "=== RECURSIVE LIST ==="
          find . -maxdepth 3 -not -path '*/.*'

      - name: 📄 Show content of pyproject.toml
        run: |
          echo "---------------------------------------------------"
          cat pyproject.toml
          echo "---------------------------------------------------"

      - name: 🧐 Check for setup.py
        run: |
          if [ -f setup.py ]; then
            echo "⚠️ WARNING: setup.py FOUND!"
            cat setup.py
          else
            echo "✅ setup.py not found."
          fi

      - name: 🐍 PIP Debug
        run: |
          pip install --upgrade pip
          # فقط ببینیم پیپ چه چیزی را برای نصب لیست می‌کند (Dry Run)
          pip install . --dry-run --report report.json
          cat report.json
